/**
 * Functions are simply transformation. it reveives an imput and transform them to somthing else.
 * Inputs and Outputs are sets of values.
 * Functions can only map a value from input ton only one value to the output universe.
 * Function is Deterministic if : Every time calling a function we're expecting to return the same value.
 * Functions should not have side effects.
 * Pure function: has no side effects, deterministic and total.
 * Varaiables are immutable, we only create new values from old ones.
 * Referential Transparency: you can replace a function with its definition
 */